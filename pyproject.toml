[project]
name = "kumpel"
version = "0.1.0"
description = "LLM-assisted German language learning utility."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.1",
]
